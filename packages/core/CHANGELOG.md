# @hypospray/core

## 0.0.1

### Patch Changes

- Add packaging metadata for the first npm release: license, repository and
  homepage links, `publishConfig.access`, `engines.node`, and a `prepack` build
  so a publish from a clean checkout always ships a fresh `dist`.
